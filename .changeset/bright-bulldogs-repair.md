---
"@zmice/zc": minor
---

Refactor `@zmice/zc` into the monorepo operator CLI.

- add `toolkit`, `platform`, and `upstream` command groups
- move prompt asset discovery to `@zmice/toolkit`
- align the runtime package with the new workspace layout
