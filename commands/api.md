## 概述
设计和审查 API 接口，确保一致性、易用性和向后兼容性。

## 设计原则
1. **一致性** — 统一的命名、错误格式、分页方式
2. **最小惊讶** — API 行为符合调用者的直觉
3. **向后兼容** — 变更不破坏现有调用者
4. **自描述** — 好的 API 设计减少文档依赖

## 输出内容
- 接口签名（输入/输出/错误码）
- 数据模型定义
- 边界条件和错误场景
- 版本策略建议
- 示例请求和响应

## 使用方式
描述你的 API 需求（RESTful / GraphQL / RPC / SDK），我将设计接口方案。
