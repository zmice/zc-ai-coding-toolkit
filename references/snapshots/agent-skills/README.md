# Agent Skills Snapshots

This directory stores immutable governance snapshots for the tracked external upstream
`agent-skills`.

Rules:

- append-only
- no in-place edits
- use snapshots as diff baselines, not as runtime input
