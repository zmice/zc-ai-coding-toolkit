# Legacy Root Source Model Snapshots

This directory is the immutable snapshot root for the seed `legacy-root-source-model`
entry in `references/upstreams.yaml`.

Use timestamped files here once concrete migration comparison points are captured.
