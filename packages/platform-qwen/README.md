# @zmice/platform-qwen

`@zmice/platform-qwen` generates and installs Qwen-facing artifacts from `@zmice/toolkit`.

Current outputs include:

- `QWEN.md`
- `qwen-extension.json`

This package does not own prompt content. It only renders platform-specific artifacts from the shared toolkit manifest.
