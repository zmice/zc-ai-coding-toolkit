调用 `safety-guardrails` 的 Guard 模式，组合启用预警和冻结，提供最高级别防护。

## 核心动作

1. 自动开启危险命令预警
2. 自动冻结默认敏感路径，或用户指定路径
3. 对环境变量、数据库操作和大范围写入施加更严格限制
4. 保留审计痕迹，直到显式解除

## 边界说明

- `guard` 覆盖 `careful` 和 `freeze`
- 适合生产、迁移、批量变更和高风险阶段

## 使用方式

在接近生产环境、处理敏感配置、或需要最高级别操作保护时使用 `/guard [路径...]`。
