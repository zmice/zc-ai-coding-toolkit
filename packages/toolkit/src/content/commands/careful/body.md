调用 `safety-guardrails` 的 Careful 模式，对高风险操作启用预警和确认。

## 核心动作

1. 识别危险命令和高风险文件修改
2. 在执行前展示风险等级、影响范围和可逆性
3. 提示更安全的替代方案
4. 高风险操作需要显式确认

## 边界说明

- `careful` 只负责预警
- 如果要锁文件，用 `freeze`
- 如果要同时启用预警和冻结，用 `guard`

## 使用方式

在准备执行数据库操作、批量修改、删除或任何你不完全确定后果的命令前使用 `/careful`。
