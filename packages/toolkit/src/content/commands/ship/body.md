调用 `shipping-and-launch`，对发布前状态做门禁检查，并明确回滚方案。

## 核心动作

1. 检查代码质量、验证状态和审查结果
2. 检查安全、性能和基础设施准备情况
3. 确认文档、变更说明和回滚方案
4. 输出发布就绪结论和剩余阻塞项

## 边界说明

- `ship` 是发布前入口命令
- 更完整的上线策略、灰度和监控方案由 `shipping-and-launch` 负责

## 使用方式

在准备上线、设计灰度发布或编写回滚预案时使用 `/ship`。
